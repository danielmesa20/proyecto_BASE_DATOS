<div class="container p-4">
    <h1>Editar datos avión</h1>
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/aviones/edit/{{avion.numero_avion}}" method="POST">
                        <div class="form-group">
                            <input type="number" value="{{avion.numero_avion}}" class="form-control" name="numero_avion" placeholder="Ingrese su cedula" disabled>
                        </div>
                        <div class="form-group">
                            <input type="text" value="{{avion.fabricante}}" class="form-control" name="fabricante" placeholder="Ingrese su nombre">
                        </div>
                        <div class="form-group">
                            <input type="text" value="{{avion.modelo}}" class="form-control" name="modelo" placeholder="Ingrese su numero de departamento">
                        </div>
                         <div class="form-group">
                            <input type="number" value="{{avion.velocidad_max}}" class="form-control" name="velocidad_max" placeholder="Ingrese su numero de departamento">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block"> Actualizar </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>